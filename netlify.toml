[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA Redirect: semua route diarahkan ke index.html agar React Router bekerja
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
